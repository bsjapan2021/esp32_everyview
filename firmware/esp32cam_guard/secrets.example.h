#pragma once
// ============================================================================
//  secrets.example.h  —  키 "이름"만 담은 템플릿 (커밋 O)
//
//  ▶ 사용법: 이 파일을 같은 폴더에 `secrets.h` 로 복사한 뒤 실제 값을 채운다.
//     cp secrets.example.h secrets.h
//  ▶ secrets.h 는 .gitignore 대상 — 절대 커밋하지 말 것 (FR-5.0.5).
//
//  ⚠️ 이 값들은 "개발 편의용 임시 주입"이다. 운영 정식 경로는
//     캡티브 포털 입력 → NVS 저장. 포털에서 값을 넣으면 아래 하드코딩은 무시된다.
//     (portal 모듈은 NVS에 저장된 값을 우선 사용)
// ============================================================================

// 텔레그램 봇 토큰 (형식: "8875648089:AAE0...qew")
#define SECRET_TG_TOKEN     ""

// 텔레그램 수신자 숫자 chat_id (@사용자명 불가 — 반드시 숫자). 비우면 자동 페어링(FR-5.0.3).
#define SECRET_TG_CHAT_ID   ""

// 클라우드/로컬 API 인증용 device_key (32자). 비우면 부팅 시 자동 생성.
#define SECRET_DEVICE_KEY   ""

// 캡티브 포털 AP 비밀번호(WPA2 8자+). 비우면 config.h 의 플레이스홀더 사용.
//  ※ 공개 저장소 노출 방지를 위해 실제 값은 여기(secrets.h)에만 둔다.
#define SECRET_AP_PASSWORD  ""
